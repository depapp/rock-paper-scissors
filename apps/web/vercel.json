{
  "buildCommand": "cd ../.. && npm install && cd apps/web && npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --production=false",
  "framework": "nextjs"
}
